# 模型评测记录

本文件记录真实模型调用及其效果。每次模型、提示词、参数或输入基准发生变化时追加记录，不覆盖历史结果。

## 记录模板

```text
日期：
Provider / 模型：
输入基准：
提示词版本：
关键参数：
结果：
耗时与用量：
指定元素保留：
原图与主体保留：
改款深度：
可生产性：
视觉质量：
已知问题：
结论与下一步：
```

正式模型选型必须对同一模型至少测试3次，再按照 `docs/ai-models.md` 的评分规则形成结论。

## 2026-08-26：阿里云百炼万相首次成功测试

- Provider / 模型：Alibaba Bailian / `wan2.7-image-pro`
- 输入基准：深靛蓝牛仔短外套商品图，带黑白格纹翻折袖口。
- 提示词版本：`garment-redesign-v1`，对应 `prompts/retro-workwear.txt`。
- 任务目标：整体改成复古铁路工装款，保留格纹袖口。
- 关键参数：Base64 单图输入、`size=2K`、`n=1`、`watermark=false`、同步调用。
- 结果：成功生成并下载一张图片，输出尺寸 `1883×2226`。
- 耗时与用量：约17秒；`image_count=1`、`input_tokens=9672`、`output_tokens=2`。
- 指定元素保留：两只黑白格纹袖口明显保留。
- 原图与主体保留：深靛蓝牛仔、悬挂商品图、衣架和背景基本保留。
- 改款深度：生成灯芯绒翻领、四口袋、工装结构线、复古明线和金属扣。
- 可生产性：结构较规整，初步适合档口快速衍生场景。
- 视觉质量：清晰，面料和缝线细节可辨。
- 已知问题：只有一次样本，不能判断稳定性；未记录实际人民币费用；部分原图姿态发生变化。
- 结论与下一步：API 链路可用于正式产品原型；需再测试至少2次并与 Seedream 使用同一基准横向比较。

## 2026-08-26：火山方舟 Seedream 接入尝试

- Provider / 模型：Volcengine Ark / `doubao-seedream-5-0-260128`
- 输入基准：同一件深靛蓝牛仔短外套。
- 提示词版本：`garment-redesign-v1`。
- 关键参数：Base64 单图输入、`size=2K`、单图、无水印。
- 结果：API Key 鉴权成功；模型调用未执行。
- 阻塞：账号尚未开通目标模型服务，接口返回模型未激活错误。
- 费用：未生成图片，不作为成功评测样本。
- 结论与下一步：开通模型后重新运行，至少生成3个样本再评分。

## 2026-08-26：Qwen 视觉分析与万相闭环探索

- Provider / 模型：Alibaba Bailian / `qwen3-vl-plus` + `wan2.7-image-pro`。
- 输入基准：同一件深靛蓝短外套，用户要求保留黑白格纹翻折袖口并进行整体改款。
- 分析结构：初版自由结构，以及带 `visible` / `inferred` / `unknown` 的严格分析实验。
- 结果：两次 Qwen 分析均能输出设计方向；严格版能够显式区分证据等级，随后万相可根据方向生成结果图。
- 耗时与用量：初版 Qwen 约56秒、总用量约4876 tokens；严格版约110秒、总用量约6955 tokens。该记录来自开发探索脚本，未记录完整费用。
- 已知问题：严格提示后仍出现口袋、衣长、肩型或面料的推断；万相在非对称口袋等结构硬约束上仍可能生成左右重复部件。
- 结论：单靠模型自报证据等级不够，必须在代码中加入证据门和确定性 Prompt Compiler；默认流程应让用户先选择方向，不自动触发生图。
- 限制：本次不是固定参数的受控 A/B，不能据此比较最终效果优劣；探索脚本位于临时目录，不属于正式应用代码。
- 下一步：正式实现使用 `garment-dna-v0.2`，只允许高置信度 `visible` 事实进入提示词；选取至少3张固定服装图，分别跑“直接生成”和“分析后生成”，记录约束遵循、原款保留、改款深度、生产性、耗时与费用。

### 后续状态更新

- `garment-dna-v0.2`、0.75证据门、Qwen VL Provider 和确定性 Prompt Compiler 已于同日进入正式代码。
- 上一条记录中的“正式实现”已完成；剩余下一步是至少3张固定样本的直接生成/分析后生成付费 A/B，不得把模拟集成测试记为真实效果评测。

## 2026-08-26：3张固定样本首轮真实 H5 A/B

- Provider / 模型：Alibaba Bailian `qwen3-vl-plus` + `wan2.7-image-pro`。
- 运行入口：当前 Taro H5，通过正式 `/api/v1/analyses` 与 `/api/v1/generations` 路由执行；快速衍生、中改、单图、系统推荐方向。
- 输入基准：白色绗缝短夹克、浅色不对称系带外套、深棕系带 A 字外套。前两张保留商家型号文字，用于验证水印干扰。
- 提示词版本：直接生成 `garment-redesign-v1`；分析后生成 `garment-analysis-v1`，分析结构 `garment-dna-v0.2`。
- 调用数量：万相成功生成5张，其中直接生成3张、分析后生成2张；Qwen 共调用7次，其中3次返回合法结构、4次因结构校验失败而停止。一次合法分析因当时客户端约60秒超时而未返回页面；失败分析未触发生图。
- H5 链路问题：首次测试发现 Taro H5 默认携带凭据导致跨端口预检后不发送 POST；修复为匿名上传 `withCredentials=false`。随后发现默认约60秒客户端超时会丢失已在服务端成功完成的分析，已调整为180秒。
- Qwen 结构问题：初始返回出现错误 `schemaVersion`、非数组字段和非法 `changes.area`；强化 JSON 骨架和数组约束后仍偶发非法 `productionRisk.level` 与 `changes.area`。第二张样本最终未取得可用分析结果。
- 样本1直接生成：约15.1秒，保留白色菱形绗缝、中央拉链和短款识别度；重做领部、纵向分割、口袋与袖口；商家型号未复现，但整体改款偏保守。
- 样本1分析后生成：Qwen 成功分析约69.4秒，证据汇总为13项采纳、2项待复核、1项未知；万相约17.0秒。结果增加腰部分割、斜插袋和抽绳袖口，更贴近推荐方向，但完整复现商家型号水印，且部分推荐的 Y 型前片没有落实。
- 样本2直接生成：约14.7秒，腰带和两侧黑色花纹袖口得到保留，但正面商品图被改成背面，不对称门襟完全丢失，并复现商家型号水印。
- 样本2分析后生成：两次分析分别因非法风险等级和非法 `changes.area` 未通过结构校验，没有触发生图。
- 样本3直接生成：约15.0秒，保留深棕单色、宽腰带和 A 字下摆，增加立领、纵向前片与新袖口，构图稳定。
- 样本3分析后生成：Qwen 约82.5秒，证据汇总为13项采纳、1项待复核、2项未知；万相约15.6秒。结果增加斜向前片和不对称叠门襟，结构方向比直接生成更明确，但推荐的角形领部未完全落实。
- 水印结论：来自带商家型号输入的3张生成结果中有2张复现文字；现有“无关文字、额外水印”负面约束不足。
- 整体结论：两阶段流程在成功样本上能提升设计方向的明确度，但当前还不能证明稳定优于直接生成。Qwen 严格结构成功率、万相水印复现和正面视角保持是继续扩大 A/B 前必须解决的问题。
- 耗时与费用：成功调用耗时已记录；本轮调用发生时应用日志尚未保存 Provider 用量或人民币费用，因此无法形成可靠成本评分。测试后已为后续成功请求补充标准化用量日志；生成图片保存在被 Git 忽略的本地 `var/assets/`，不提交仓库。
- 下一步：先加强结构化输出约束、中文方向输出、水印忽略和正面视角硬约束，再使用完全相同的3张样本复测；复测通过后才执行每条路径3次的稳定性评测。

## 2026-08-26：`qwen3.7-plus` 严格结构升级验证

- Provider / 模型：Alibaba Bailian `qwen3.7-plus`，OpenAI 兼容接口，非思考模式。
- 变更原因：旧 `qwen3-vl-plus` 的 JSON Object 模式只能保证合法 JSON，首轮7次调用中有4次因字段类型或枚举不合规被领域 Schema 拒绝。
- 输出策略：使用模型原生严格 JSON Schema；下游仍执行 `garment-dna-v0.2` Zod 校验和证据门。对无歧义的数组、枚举、版本号与内部 ID 偏差先确定性归一化，仍失败时最多执行一次不重传图片的文本修复，两次调用共享150秒总预算。
- 输入基准：与首轮 A/B 完全相同的白色绗缝短夹克、浅色不对称系带外套、深棕系带 A 字外套；前两张仍保留商家型号文字。
- 结果：3张样本均在首次请求返回合法结构，修复请求触发0次；本轮结构成功率为3/3。
- 耗时：三次分别约44.7秒、40.6秒、42.2秒，平均约42.5秒；均低于旧模型两次成功样本约69.4秒和82.5秒。
- 用量：三次总计输入17590 tokens、输出7104 tokens、合计24694 tokens；单次总量分别为8524、8693、7477 tokens。
- 内容观察：三个方向名称均使用中文；前两张样本的分析 JSON 未出现商家型号字符串。该结果只说明分析层忽略了型号，不能证明后续万相不会从原图复现文字。
- 限制：本次是真实 Provider 调用，不是完整 H5 两阶段生图；仅3次样本且未触发修复路径，不能宣称生产环境100%成功，也不能与中途修改过提示词的旧7次调用做严格统计对比。
- 结论与下一步：将默认分析模型切换为 `qwen3.7-plus`。使用新版分析重新完成同一3张样本的万相生图，然后扩大样本和重复次数，记录严格结构成功率、修复触发率、方向质量、总时延和费用。

## 2026-08-26：`qwen3.7-plus` + 万相新版完整链路复测

- Provider / 模型：Alibaba Bailian `qwen3.7-plus` + `wan2.7-image-pro`。
- 运行入口：正式 `/api/v1/analyses` 与 `/api/v1/generations` 路由；每张图选择系统推荐的 `direction-1`，经过证据门、确定性 Prompt Compiler、万相生成和本地转存。
- 输入基准：与前两轮相同的3张固定女装外套图和相同用户约束。
- 技术结果：3次 Qwen 均首次返回合法结构，修复触发0次；3次万相均成功生成，完整链路技术成功率3/3。
- 样本1：推荐“现代极简轻户外风格”；Qwen约43.1秒，万相约11.8秒。保留白色绗缝、中央拉链和短款比例，增加大翻盖袋、弧形前片和翻折袖口；改款幅度仍偏保守，且输出复现商家型号并把其中一个数字改错。
- 样本2：推荐“极简通勤风”；Qwen约40.0秒，万相约11.5秒。保持正面视角、不对称领口门襟、腰带和黑色花纹袖口，修复了旧直接生成的正面变背面问题；但整体与原款非常接近，且完整复现商家型号。
- 样本3：推荐“现代极简结构感外套”；Qwen约47.2秒，万相约11.5秒。保留深棕色、宽腰带和 A 字比例，新增立领、弧形前片与更利落的直袖，三张中结构改款最明确。
- 用量：Qwen三次合计24954 tokens；万相三次合计28572 tokens并生成3张，输出尺寸依次为1773×2365、1556×2693、1773×2364。
- 水印结论：Qwen 分析没有把商家型号写进结构，但万相仍会直接从参考图复制像素；新版两张带字输入的结果2/2复现型号，说明仅靠“无关文字、额外水印”的负面提示无法解决，需要在传给生图模型前处理文字区域。
- 整体结论：升级模型和严格 Schema 已解决本轮结构失败，完整链路从旧版2/3提高到新版3/3；产品效果仍不能判定完全通过，当前首要问题转为输入水印预处理，其次是改款深度和方向落实度。

## 2026-08-26：`qwen-image-2.0-pro-2026-06-22` 生图对照

- Provider / 模型：分析继续使用 Alibaba Bailian `qwen3.7-plus`；生图切换为 `qwen-image-2.0-pro-2026-06-22`，默认万相配置不变。
- 运行入口：正式 `/api/v1/analyses` 与 `/api/v1/generations` 路由，系统推荐 `direction-1`，经过证据门、确定性 Prompt Compiler、Qwen Image 编辑和本地转存。
- 参数：单图、`n=1`、`watermark=false`、`prompt_extend=false`；未指定 `size`，保持输入长宽比并使用模型默认约1MP输出；额外正向要求删除原图文字，并通过 `negative_prompt` 禁止型号、货号、价格、水印和任何可读文字。
- 成本边界：官方原价为0.5元/张，三张理论原价合计1.5元；实际账单和优惠未在本轮读取。官方限流为2次/分钟，本轮顺序执行。
- 技术结果：3次分析和3次生图均成功，完整链路3/3；Qwen Image 生图耗时约18.0秒、16.6秒、16.3秒，平均约17.0秒。
- 样本1：保留白色菱形绗缝、中央拉链和短款比例，增加立领、上下材质分区、非对称拉链口袋和机能袖口；改款深度明显高于同轮万相，商家型号完全消失，但商品图背景由木纹改为白色。
- 样本2：保留正面视角、不对称领口门襟、腰带和黑色花纹袖口，商家型号完全消失；整体仍非常接近原款，改款深度不足。
- 样本3：保留深棕单色、宽腰带和 A 字下摆，增加立领和外扩袖口；结构简洁可生产，但前片分割变化弱于同轮万相。
- 分辨率：三张依次为896×1184、768×1344、896×1184，约1MP，明显低于同轮万相约4MP结果；后续需评估显式高分辨率 `size`，同时保持原图比例。
- 水印结论：两张带商家型号的输入0/2复现文字，对照万相同轮2/2复现，Qwen Image 在当前提示和参数下显著更好；样本量仍不足以宣称稳定解决。
- 整体结论：Qwen Image 值得保留为正式候选 Provider，当前优势是去除原图文字和样本1的改款深度，劣势是默认分辨率、单张原价和部分样本改款保守。下一步应在相近分辨率下对两种生图模型各重复3次，再决定默认 Provider。

## 2026-08-26：Qwen Image 真实结果迭代首测

- Provider / 模型：分析使用 Alibaba Bailian `qwen3.7-plus`；首次生成、同方向再生成和两次继续修改均使用 `qwen-image-2.0-pro-2026-06-22`。
- 输入基准：白色短款菱形绗缝夹克固定样本。硬约束为白色菱形绗缝面料、中央金属拉链和短款比例；要求删除右下角商家型号文字并形成现代轻户外机能风格。
- 运行入口：正式 `/api/v1/analyses`、`/api/v1/generations`、`/api/v1/generations/:jobId/refinements` 和结果资源接口；执行“分析 → 首次生成 → 同方向再生成 → 连续修改2次 → 下载”。为遵守 Qwen Image 2 RPM 限制，第三次生图前主动等待约26秒。
- 技术结果：1次分析和4次生图全部首次成功，没有429、超时、结构修复或重试。分析约39.2秒，4次生图模型耗时依次约16.4、16.4、19.0、19.0秒；整段含主动限流等待约141.5秒。最终资源下载返回200、PNG约1.69 MB，父子任务和操作类型均正确。
- 用量与成本边界：分析使用6069输入 tokens、2190输出 tokens、合计8259 tokens；4张图片均为896×1184。按北京地域官方原价0.5元/张计算，生图理论原价2元，实际账单和分析费用未读取。
- 首次生成：白色、绗缝、中央拉链和短款比例均保留，新增连帽、前片分割和拉链口袋；商家型号消失，可作为候选结果。
- 同方向再生成：技术和约束均成功，商品仍为白色短款绗缝连帽夹克，但主要差异偏向面料光泽和纹理强弱，设计多样性一般。
- 第一次继续修改：要求放宽袖型并加强机能袖口。结果仍无文字，主要结构和白色大体保留，但袖型/袖口变化不明显，开始出现轻微偏色和更强反光，指令落实度未通过。
- 第二次继续修改：要求保持上一版袖口并增加克制的非对称斜向分割和单个实用拉链袋。结果未按要求形成清晰的非对称结构，且全图出现明显彩色噪点和色彩漂移，视觉质量不通过。
- 水印结论：原图商家型号在4张结果中0/4复现，继续支持 Qwen Image 在当前样本上具备较好的文字清除能力。
- 整体结论：接口、幂等、父子记录、同方向再生成、两次继续修改和下载组成的技术链路已打通，但不能据此宣称结果迭代产品闭环完成；本轮两次局部修改0/2达到完整视觉验收，主要问题是最新指令关注不足和多代单图编辑累积漂移。
- 测后优化：将本轮指令移到迭代 Prompt 前部；从第二次修改开始，同时向支持多图输入的 Provider 传入分支首次结果作为稳定基准图和上一版作为编辑底图。官方 Qwen Image 编辑接口支持多图输入，且多图输出比例跟随最后一张输入，因此实现保持上一版为最后一张。该改动已通过领域、Provider 和服务端自动化测试，尚未追加付费实测，不得记录为效果已修复。

## 2026-08-26：Qwen Image 双图锚定复测

- 复测目的：验证上一轮测后增加的“本轮指令前置 + 第二次修改使用稳定首版和上一版双图输入”能否阻止连续编辑画质漂移。
- 输入与指令：继续使用白色短款菱形绗缝夹克和相同硬约束；先要求放宽袖型、加强机能收束袖口，再要求保留袖口并增加克制的非对称斜向分割和实用拉链口袋。
- 运行入口：正式分析、首次生成、两次继续修改和结果资源下载接口。第二次修改日志确认 `referenceImageCount=1`，稳定锚点为本轮首次生成任务，说明双图代码真实生效。
- 技术结果：分析和3次生图全部首次成功，没有429、超时、结构修复或重试。分析约63.3秒；首次生成、第一次修改和双图第二次修改分别约18.1、18.9、28.6秒；整段含约23秒主动限流等待约154.5秒。最终PNG下载返回200、约1.60 MB。
- 用量与成本边界：分析使用6069输入 tokens、3522输出 tokens、合计9591 tokens；3张输出均为896×1184。按北京地域官方原价0.5元/张计算，生图理论原价1.5元，实际账单和分析费用未读取。
- 首次生成：结果为干净白底的白色短款绗缝机能夹克，保留中央拉链和菱形绗缝，增加高立领、非对称胸袋、袖袋、纵向结构线和复合袖口；无商家型号，可用。
- 第一次修改：服装结构大体继承首版，但全图已经出现明显过度锐化、暗边和灰黑颗粒背景；袖型与袖口变化不够明确，视觉质量不通过。
- 第二次修改：请求真实携带稳定首版和退化上一版两张输入，但噪点、色偏和海报化进一步增强，未形成清晰的新斜向分割，视觉质量不通过。双图锚定没有阻止退化。
- 结论：根因更接近“生成图再次作为图像编辑输入”的代际退化，而不是接口、时延、限流或是否提供稳定参考图。官方接口只支持单轮图像编辑调用，应用侧父子链不会形成厂商模型的原生多轮上下文。
- 测后策略：停止把上一轮模型输出作为下一轮像素输入。保留父子历史和累计指令，但每次继续修改都从分支首次成功结果重新生成，并在单次 Prompt 中完整执行所有累计修改。该稳定重基代码已实现并增加自动化断言，尚未追加付费调用，不能记录为效果已修复。

## 2026-08-26：Qwen Image 稳定首版重基聚焦测试

- 测试目的：排除上一版退化图的干扰，只使用上一轮完整测试中保存的干净首次生成结果，在一次请求里执行相同的两条累计修改。
- 输入：干净白底的白色绗缝机能夹克首次结果；累计要求为放宽袖型、加强机能袖口，并增加克制的非对称斜向分割和实用拉链口袋。
- 调用方式：当前 `garment-iteration-v1` Prompt Compiler 配合 Qwen Image Provider 的单次聚焦调用，不重新分析，不经过父子路由。
- 技术结果：请求首次成功，模型约17.0秒、端到端约17.9秒，输出896×1184；按北京地域官方原价计算理论费用0.5元，实际账单未读取。
- 视觉结果：没有进一步累积成上一轮第二次修改的彩色噪点，但依旧立即出现明显过度锐化、深色轮廓和大面积灰黑背景颗粒；两条结构修改的增量不清晰，视觉质量不通过。
- 结论：即使只编辑一次干净的 Qwen 首次生成结果，也会复现第一次修改的退化特征。根因可收敛到“Qwen生成图再次作为Qwen图像编辑输入”，稳定首版重基不能解决。
- 测后策略：客户端继续修改时重新上传最初商品图；服务端校验原图 SHA-256，并用原始选中方向、硬约束和全部累计指令从原图一次性重建。父子记录仍用于历史与累计指令，但任何模型生成结果都不再送回 Provider。该代码已实现，尚未真实付费复测。

## 2026-08-26：Qwen Image 原图重建聚焦测试

- 测试目的：验证完全停止编辑模型生成图后，直接以最初商品图、选中方向和全部累计指令一次性重建，能否同时解决画质退化和修改落实度问题。
- 输入：最初白色短款菱形绗缝夹克商品图；选中“都市探索者·模块化拼接夹克”方向；累计要求为放宽袖型、加强机能袖口，并增加克制的非对称斜向分割和一个实用拉链口袋。要求保留白色绗缝、中央金属拉链和短款比例，并删除商家型号文字。
- 调用方式：当前 `garment-iteration-v1` Prompt Compiler 配合 Qwen Image Provider 的单次聚焦调用，明确设置 `usesOriginalSourceImage=true`；不重新分析，不经过 H5 refinement 路由。
- 技术结果：请求首次成功，Provider 请求 ID 为 `642b7f6e-87d8-99a8-845e-d7201406dcae`；模型约18.1秒、端到端约19.4秒，输出896×1184。按北京地域官方原价计算理论费用0.5元，实际账单未读取。
- 视觉结果：商家型号完全消失，没有出现此前的彩色噪点、深色锐化轮廓或背景颗粒；白色菱形绗缝、中央拉链、短款比例和正面商品图构图保留良好。结果增加高立领、非对称胸部拉链袋、袖袋、纵向与斜向分割、宽袖和收束袖口，所选基础方向及两条累计修改均有可见落实，结构具备基本可生产性。
- 保留问题：袖口更接近松紧收口而非更复杂的功能调节结构；斜向分割较克制。当前只有一个固定样本、一次调用，不能外推为稳定成功率。
- 结论：原图一次性重建1/1通过本轮人工视觉验收，显著优于单图链式、双图锚定和稳定首版重基；“继续修改”应保持父子记录与累计指令，但像素输入必须始终使用最初商品图。下一步通过正式 H5 refinement 路由复测同一策略，再扩展到多样本重复评测。

## 2026-08-27：Qwen Image 原图重建完整 H5 路由复测

- 测试目的：验证聚焦调用通过的原图重建策略在真实390×844 H5 产品流程中一致生效，并覆盖分析、方向选择、首次生成、refinement、父子历史、结果展示和下载。
- 输入与操作：上传最初白色短款菱形绗缝夹克商品图，保留白色绗缝、中央金属拉链和短款比例，要求现代轻户外机能风、高立领、非对称模块化胸袋、袖袋和纵向结构线；选择系统推荐“都市探索者风格轻户外机能夹克”。继续修改时一次提交宽松袖型、魔术贴调节袖口、克制斜向分割和实用拉链袋要求。
- 分析结果：`qwen3.7-plus` 首次返回合法严格结构，约40.3秒；采纳15项可见事实、待复核0项、未知1项；使用6047输入 tokens、2235输出 tokens、合计8282 tokens。
- 首次生成：任务 `c7b874a3-7dde-4a9c-bdab-6d295d9a89c9`，Qwen Image 约16.9秒，输出896×1184。白色绗缝、中央拉链、短款比例保留，新增高立领、胸前模块袋和袖袋，无商家型号，可用。
- refinement：任务 `8d230357-29c7-4190-b885-0810fff64027` 正确指向首次生成父任务；服务端日志明确记录 `sourceStrategy=original-image-rebuild`、`promptVersion=garment-iteration-v1` 和 `directionId=direction-1`。模型约18.8秒，输出896×1184，请求首次成功。
- refinement 视觉结果：没有强锐化、背景颗粒、偏色或文字；原款核心约束和所选方向均保留，袖型变宽且形成清楚的魔术贴调节袖口，胸袋和袖袋结构可见。非对称斜向分割仍较弱，但总体具备基本可生产性，通过本轮人工视觉验收。
- H5 与下载：页面显示“上一版/当前结果”、首次生成与继续修改两条历史，继续修改说明已明确“从原图重新生成”。下载操作未显示错误；结果资源返回200、`image/png`、1,319,281 bytes，下载文件与本地转存文件 SHA-256 一致。
- 成本边界：本轮调用1次分析和2张图片；按北京地域项目当前记录的官方原价，生图理论原价合计1元，实际账单和分析费用未读取。
- 结论：本地结果迭代产品闭环已在一个真实 H5 固定样本上打通，原图重建策略同时通过技术和视觉验收。该结果不能外推为多样本稳定成功率，也不代表对象存储、异步任务、清理、限流、预算、内容安全或手机真机保存已经完成。
