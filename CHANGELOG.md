# 变更记录

本项目遵循 Keep a Changelog 的组织方式。在正式发布版本前，所有用户可感知变化先记录在 `Unreleased`。

## Unreleased

### Changed

- 结果页增加“原图/上一版、双图对照、当前结果”三态查看，默认用双图对照帮助判断每轮差异；同时展示确定性的版本说明，区分首次生成、同方向重新生成和继续修改，并明确这些文字是发送给生图模型的设计依据，不冒充已自动核验的视觉事实。历史版本可被设为当前查看版本并从该版本继续修改，建立新分支而不覆盖后续结果。
- 分析、首次生成和继续修改增加阶段式等待反馈，显示已等待时间、保守预计和当前处理步骤；这些阶段由客户端按耗时估算，不冒充 Provider 实时进度。失败提示按原图失效、权限、额度、输入错误、明确可重试和任务状态不确定分别给出下一步操作；状态不确定的付费任务不会引导连续提交，小程序会提示可恢复任务，H5 不再误称具备同等恢复能力。
- 客户端首页重构为“改款工作台”三阶段界面：原款与要求、设计方向、结果与版本不再同时堆叠在一条长页面中；结果页改为大图优先、原图/当前结果切换，以及“改一下 / 再出一版 / 保存图片”三个明确动作。锁定项候选默认折叠，选中方向默认收敛为改款幅度、变化数量和继承锁定数量，完整改款清单按需展开，专业详细核对继续保留为可选入口；第二轮视觉打磨采用清洁暖灰画布、象牙白主卡和中性灰图片区，减弱嵌套分隔与大面积强调色。模型、Prompt、网关、额度和云端协议均未改变。
- “需要修改”现在以自由文字为主、常见问题勾选为辅，两者由客户端确定性合并为最多500字的继续修改要求；填写和勾选不调用模型，只有用户点击“生成修改后的下一版”才占用1次生图额度。微信小程序恢复云端结果后可复用父任务持久化的原始商品图，不再要求本机仍保留临时图片；原图过期时返回稳定 `PARENT_ASSET_EXPIRED` 且不占用新额度。H5 继续保持重新选择原图的安全边界。
- 上述自由文字反馈和父任务原图复用能力已经使用 CloudBase“仅更新函数代码”方式部署到现有微信云环境，线上私密 Provider、模型 Key、额度和体验窗口未由仓库安全默认值覆盖；微信云网关小程序产物已重新构建，并已在真机完成一次“只输入自由文字、不重新上传原图”的真实继续修改验收。
- 分析后增加可确认的保留项：只把证据门采纳的高置信度可见事实作为候选，用户明确选择或补充后才进入 H5/微信共享生图请求和确定性 Prompt；方向卡改为移动端渐进展开，显示改款幅度、继承保留项、变化维度、具体改款理由和生产风险。结果页默认改为“满意/需要修改”两步反馈，需要修改时再勾选问题并自动整理继续修改指令；“通过/存疑/未通过”的逐项核对降为可选专业入口，不额外调用付费模型，也不冒充自动视觉验收。
- 微信云体验权限增加带截止时间的 `wechat-experience` 模式：体验版测试者无需再提交 OpenID 指纹；截止时间到达、配置缺失或模式无效时自动回退 `trial_members` 指纹白名单，仓库部署默认继续失败关闭。
- 微信云函数接入正式 `qwen3.7-plus` 分析与 `qwen-image-2.0-pro-2026-06-22` 生图 Provider，复用证据门、确定性 Prompt Compiler 和原图重建式继续修改；真实环境已完成一次付费真机分析、首次生成和继续修改验收，三次调用均首次成功。个人使用阶段的云端用户与全局日上限均调整为50次分析、50次生图；仓库部署默认值继续保持 Fake。
- 微信云任务执行输入升级为 `garment-generation-v2`，持久化 Provider 模式、源图 SHA-256、Prompt 版本与内容、基础请求指纹、父任务和累计修改；执行前重新读取并校验源图，配置切换时停止任务以避免错误计费。
- 微信云分析在 Provider 调用前原子预占用户/全局额度并写入源资产和幂等记录；客户端仅自动重试明确的云函数传输错误，并在同一页面复用处理中分析的上传引用和幂等键。
- 微信云函数上限调整为180秒，真实 Provider请求预算为150秒，生成执行租约为210秒；无效真实 Provider 配置会关闭业务并返回稳定错误，不会在函数启动阶段直接崩溃。
- 微信云 Fake 生图改为真正的两阶段任务：创建动作只持久化执行输入并立即返回 `queued`，首次任务查询通过事务租约领取执行权后再转存结果；并发查询和同键重放仍只执行一次、只占一次额度。
- 微信测试环境为两阶段恢复验收增加可配置的 Fake 执行延迟；该延迟仅在显式 Fake Provider 下生效，接入真实 Provider 后移除。
- 微信云生成任务增加事务执行租约：模型调用前中断允许同请求在租约过期后接管；模型调用开始后的中断转为稳定失败，不自动重打 Provider，避免潜在重复计费。
- Fastify 首次生成与继续修改入口改用共享 `GenerationTaskAdmissionService`，在一次事务准入中完成每日生图额度、排队任务和幂等绑定；H5 的异步任务与错误码行为保持不变。
- 本地开发默认生图 Provider 改为 Alibaba Qwen Image `qwen-image-2.0-pro-2026-06-22`；万相仍可通过服务端环境变量显式切换，用于回退和后续 A/B。
- 创建改款和继续修改由长连接同步响应改为“创建任务后轮询状态”；分析接口本轮仍保持同步。
- 连续修改改为重新上传并校验最初商品图，每轮从原图、选中方向和全部累计指令一次性重建结果；不再把任何模型生成图作为下一轮编辑输入。
- 生成幂等记录增加请求指纹校验；同一幂等键不能用于不同请求，失败执行仍可安全重试。
- 澄清目标需求、目标架构、当前实现和上线前硬门槛的区别，增加实施状态矩阵，避免将下载、历史、异步任务和删除能力误认为已经完成。
- 服装分析默认模型由 `qwen3-vl-plus` 升级为原生多模态 `qwen3.7-plus`，关闭思考模式并使用严格 JSON Schema。
- 强化 Qwen 服装分析提示词，明确忽略商家型号、水印和价格文字，要求所有用户可见内容使用简体中文。
- 分析结果先进行证据安全的确定性归一化；仍不符合结构时，在150秒总预算内执行一次不重传图片的定向文本修复，并汇总调用用量。
- Provider 结构校验失败时仅记录 Zod 错误码和字段路径，便于诊断且不记录原图或完整模型响应。
- 成功的分析和生图日志增加标准化用量字段，便于后续记录 token、图片数量和输出尺寸。

### Fixed

- 修正 H5 下 Taro 将 `disabled={false}` 渲染为 `disabled="false"` 时仍命中属性选择器、导致可用按钮显示成禁用态的问题；禁用样式现在只匹配真实禁用值。新版工作台同时把390px视口正文提高到约14–16px、主要操作提高到约44px以上，并修正 H5 `aspectFit` 结果图未居中的问题。
- 修正微信小程序按750设计稿转换后分析与结果区正文、辅助说明和操作文字过小的问题；微信端先单独提高返回摘要、方向卡、问题反馈、继续修改和历史区域的最低可读字号，后续工作台重构再统一校准 H5 移动视口字号。
- 隔离 H5 与微信小程序构建目录，并在整仓构建中校验两端入口文件，避免 H5 产物覆盖小程序 `app.json` 导致真机调试失败。
- 修复旧页面轮询完成后抢先清除设备 jobId、导致新页面无法恢复同一云任务的竞态；终态现在只由新页面成功接管后确认清理，且旧任务不能清除更新的待处理任务。
- 修复小程序恢复云端成功任务后因本地原图临时路径已丢失而完全不展示结果的问题；恢复页现在单独展示当前结果并允许保存，同时明确提示重新修改需要重新上传原图。
- 小程序保存结果时将相册权限拒绝、云文件下载失败和其他保存失败转换为可操作的中文提示，不再直接暴露微信或云存储原始错误。
- 修复微信小程序运行时无法稳定执行共享 Zod 校验、导致真实 Fake 分析已经落库但客户端报错的问题；微信云网关改用不引入 Zod 运行时的确定性结构守卫，仍会拒绝不完整或不一致的分析响应。
- 兼容 `wx-server-sdk@4.0.2` 在云文件上传已成功且返回有效 `cloud://` 文件 ID 时仍使用 `statusCode=-1` 的状态哨兵；HTTP 明确失败和无效文件 ID 仍会被拒绝。
- 修复微信云函数产物未内联 `@cloth-idea/domain`、导致云端启动时报 `Cannot find module` 的问题；构建阶段现在会拒绝任何仍含未解析工作区依赖的部署包。
- 修正继续修改区域的说明文案，明确每一版都从原图和累计指令重建，不再误称模型会编辑当前生成图。
- 修复 Taro H5 在模型请求快速结束时，Button 内置 loading 动画与文本更新竞争导致虚拟 DOM 插入异常的问题；改用禁用状态和明确的加载文案。
- 修复匿名 H5 上传仍默认携带凭据、导致跨端口 API 预检后不发送实际请求的问题，并将长耗时模型请求的客户端超时显式调整为 180 秒。

### Added

- 增加微信云每日过期清理触发器和共享清理用例；先保护仍有有效引用的共享文件，物理云文件删除成功后才移除过期资产元数据，失败记录保留重试，并同步清理过期分析、任务和幂等记录。真实触发器已经删除1个专用过期文件和对应元数据，复核文件404且记录为空。
- 在独立微信云环境创建并以 `ADMINONLY` 保护五张业务集合，部署显式启用的 Fake Provider 配置；真实微信身份已跑通结构化分析、方向选择、首次生成、同方向再次生成、父子任务和结果云存储转存，全程未调用付费模型。
- 增加受环境开关保护的微信云 Fake Provider 业务闭环：支持结构化分析、首次/再次生成、继续修改和任务查询，复用持久任务、幂等、额度与资产仓储；Fake 结果明确标记为测试数据且不调用付费模型。
- `WechatCloudGarmentGateway` 接入云存储上传、同幂等键传输重试、任务轮询和未完成 jobId 的设备恢复；页面重进后继续查询任务，`cloud://` 结果使用微信云 SDK 下载并保存。
- 增加微信云数据库 `garment_analyses` 与 `garment_assets` 适配器，以及按用户和资产编号构造受控路径的云文件上传、下载、删除适配器；测试覆盖用户隔离、跨实例恢复、过期、主动删除和不安全路径拒绝。
- 增加微信云数据库 `generation_jobs`、`idempotency_records`、`trial_usage` 事务适配器；Fake 数据库契约测试覆盖并发幂等、跨适配器实例恢复、额度原子预占/回滚、状态更新和过期删除。
- 增加跨 Fastify 与微信云函数复用的 `@cloth-idea/application` 工作区，定义分析、生成任务、幂等、用户/全局额度和资产仓储端口，并提供带事务回滚的内存 Fake。
- 增加共享生成任务准入用例：在同一事务中完成额度预占、排队任务创建和幂等绑定；并发重试只占一次额度，同键换请求返回冲突，后续写入失败会回滚。
- 增加微信云端无模型费用探针：通过 OpenID 派生用户指纹和 `trial_members` 白名单保护临时上传，使用 `garment-api` 持久化、恢复并主动删除云文件与数据库记录。
- 增加微信云函数 TypeScript workspace、稳定请求/响应协议和微信端诊断入口；模型业务方法继续保持关闭，真机基础设施验收不会产生模型费用。
- 增加微信云开发部署与验收说明，记录独立环境、集合权限、体验成员加入、探针恢复和清理步骤。
- 接入新小程序 AppID 和独立微信云开发环境配置；微信构建启动时初始化指定环境，H5 构建不访问微信云运行时。
- 增加客户端 `GarmentGateway` 边界，将现有 HTTP 链路收敛为默认实现，并为尚未部署的微信云网关提供明确错误，避免静默触发错误传输或付费调用。
- 记录微信小程序体验版升级计划：独立 AppID 与云开发环境、H5/微信业务网关、OpenID 权限、持久任务与幂等、云存储清理、分阶段实施和真机验收标准。
- 增加 ADR-0008，确定下一轮受邀试用优先使用独立微信云开发环境的小程序体验版，同时保留现有 H5 与 Fastify 路径。
- 增加无需数据库的单机受控试用模式：可选共享访问码、每日分析/生图额度、全局模型并发控制和可配置生图启动间隔。
- H5 在服务端启用访问码时显示跨端输入框，并仅向本项目付费模型路由发送用户输入的访问码；客户端构建产物不包含访问码。
- 本地结果目录增加可选过期清理，默认关闭；启用后只删除超过保留期且名称为任务 UUID 的结果目录。
- 增加进程内异步生图任务，支持 `queued`、`generating`、`succeeded`、`failed` 状态与任务查询接口；Provider 失败会保存为稳定业务错误。
- H5 增加生图任务自动轮询、轮询网络错误恢复和提交传输失败的一次同幂等键重试，避免客户端断开后重复创建付费任务。
- 生成幂等升级为任务级幂等：同一请求键从排队到失败都绑定同一任务，不会因重试重复调用模型。
- 增加本地结果迭代闭环：三个方向可分别生成，同方向可再次生成，并可从原图与累计指令重建继续修改结果。
- 增加 `garment-iteration-v1` 确定性迭代提示词，累计追加修改时继续保留原始硬约束、证据门事实和禁止项。
- 生成响应增加操作类型、父任务、追加指令和创建时间；服务端在进程内保存父子生成关系。
- H5 增加原图/上一版与当前结果对比、本次页面会话历史和 Taro 跨端结果下载入口。
- 客户端增加同步的付费模型请求防重入保护，失败生成不会移除上一张成功结果。
- 增加 npm workspace、共享严格 TypeScript 配置、ESLint、Prettier、Vitest 和统一检查命令。
- 增加 Taro + React 手机端改款页面，支持灵感设计、快速衍生、原图选择、保留项、改款方向和幅度。
- 增加 H5 与微信小程序构建能力。
- 增加 Fastify 服务端及健康检查、能力查询、服装改款生成和结果文件接口。
- 增加统一领域协议、版本化服装改款提示词和生成摘要。
- 增加正式 Alibaba Wan Provider，包含超时、错误标准化和临时结果下载。
- 增加可配置的 Alibaba Qwen Image Provider，支持 `qwen-image-2.0-pro-2026-06-22` 参考图编辑、独立负面提示和结果本地转存。
- 增加并发安全的生成幂等、本地结果转存、文件内容/大小校验和8项自动化测试。
- 增加当前 API 契约文档和 Fastify/npm workspace 架构决策记录。
- 建立 `AGENTS.md`，规定新窗口阅读顺序、架构约束、开发流程和 Definition of Done。
- 建立产品需求、系统架构、模型接入、开发规范、当前状态和模型评测文档。
- 建立 Taro 跨端、服务端统一 Model Provider、H5 匿名首版三项 ADR。
- 增加 EditorConfig、Prettier 和 Markdownlint 基础格式规范。
- 增加本地 Markdown 链接检查命令 `npm run check:docs`。
- 增加阿里云百炼万相 `wan2.7-image-pro` 参考图改款验证脚本。
- 增加火山方舟 Seedream 参考图改款验证脚本。
- 增加固定服装改款基准提示词。
- 增加 `garment-dna-v0.2` 严格结构、视觉事实证据等级、置信度门控和三个结构化设计方向。
- 增加 Alibaba Qwen VL 服装分析 Provider、确定性 Prompt Compiler 和分析结果一小时有效期。
- 增加“分析原款 → 选择方向 → 生成效果图”H5 流程，展示关键改款、推荐方向和生产风险。
- 增加 `/api/v1/analyses`，并允许生成接口通过 `analysisId` 与 `directionId` 使用选中方向。
- 保留直接生成作为显式降级与 A/B 对照，不会在分析后自动触发生图或自动重试付费请求。
- 自动化测试覆盖 Qwen 严格 Schema、结构归一化、单次修复上限、证据门、首次/迭代提示词编译、父子生成、稳定基准图、多图 Provider 输入、异步任务、请求指纹幂等和分析后生图闭环。

### Security

- Fake 业务真实验收后，按精确文件路径和文档 ID 永久删除7个测试云文件与21条分析、资产、任务、幂等和额度记录；复核五张业务集合均为空，体验成员白名单保持不变。
- 微信云业务动作默认关闭；只有云函数显式设置 `WECHAT_CLOUD_BUSINESS_PROVIDER=fake` 才启用无模型 Fake 路径，未配置时返回稳定 `CLOUD_BACKEND_NOT_DEPLOYED`，不会静默触发模型或产生模型费用。
- 无模型费用探针已在独立云环境完成主动清理验收；测试图片的原云存储路径返回404，对应 `infrastructure_probes` 记录查询为空。
- 微信云环境中的 `trial_members` 与 `infrastructure_probes` 已设置为 `ADMINONLY`，小程序客户端不能绕过云函数直接读写白名单或探针记录；云存储保持仅上传者和管理员可访问的 `PRIVATE` 权限。
- 微信云端响应只返回 OpenID 的截断 SHA-256 指纹；临时上传路径绑定该指纹，云函数在持久化和删除前再次校验白名单、路径归属、类型、大小和幂等键。
- 云函数部署包固定覆盖 `wx-server-sdk` 中存在已知漏洞的传递依赖，根项目和独立云函数部署包的生产依赖审计均为0项已知漏洞。
- 受控试用的分析、首次生成和继续修改接口支持恒定时间校验访问码；错误响应和日志不包含访问码。
- 进程内额度只对新模型调用计数，同一幂等任务的重复提交不会重复占用额度。
- 模型 API Key 仅保存在被 Git 忽略的 `.env.local`。
- 模型调用只在服务端发生，客户端构建产物不包含 API Key。
- 服务端生产依赖通过 `npm audit --omit=dev` 检查，当前为0项已知漏洞。
- 明确禁止客户端、日志、文档和 Git 历史保存真实模型密钥。
- 记录公开部署前必须轮换开发期已展示 Key 的要求。
