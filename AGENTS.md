# 项目协作规则

本文件是所有新开发窗口、Codex 任务和贡献者进入仓库后的首要入口。开始工作前必须按顺序阅读：

1. `README.md`
2. `docs/current-status.md`
3. 与任务相关的 `docs/` 文档
4. `docs/decisions/` 中相关的架构决策记录

## 实施状态阅读规则

- `docs/current-status.md` 是“当前实际做到了什么”的唯一准确信源。
- `docs/product-requirements.md` 描述目标产品范围，不表示每一项都已实现。
- `docs/architecture.md` 同时包含当前实现和目标架构，必须按章节标注区分。
- 状态为 `Accepted` 的 ADR 表示方向已经接受，不表示对应能力已经全部交付。
- 若文档与代码对实施状态的描述不一致，先核对代码和测试，再修正 `docs/current-status.md`；不得仅凭目标文档宣称功能完成。

## 项目目标

本项目是一个 AI 服装设计与改款助手，服务两个核心场景：

- 设计师通过原款和约束条件寻找灵感。
- 服装档口快速衍生可生产的新款。

首版为手机端 H5，采用跨端架构，为后续微信小程序保留兼容性。产品不是简单的领型、口袋替换器，也不承诺首版直接输出纸样或完整生产工艺单。

## 已确认的产品边界

- 两种使用模式：灵感设计、快速衍生。
- 用户上传一张服装原图，填写保留项、修改目标和风格偏好。
- 系统先形成设计方向，再根据选中方向生成效果图。
- 默认一次生成一张，用户可主动请求更多方案。
- 首版输出包括设计说明、改款清单和效果图。
- 首版不包括纸样、尺寸表、完整工艺单、3D 服装和在线支付。
- H5 首版不强制登录，使用匿名会话；微信登录和跨设备历史后续增加。

## 不可违反的架构约束

- 客户端采用 Taro、React 和 TypeScript，必须同时考虑 H5 与微信小程序。
- 禁止在业务代码中直接依赖 DOM、`window`、`document` 或浏览器专用上传能力；平台能力必须经过适配层。
- 生图模型只能由服务端调用，API Key 不得进入 H5、小程序、日志、截图、提交记录或客户端构建产物。
- 所有模型通过统一 Provider 接口接入，业务层不得写死万相或 Seedream。
- 用户上传图、生成图和任务记录在受控测试前必须具备可删除、可过期和可追踪机制。当前本地验证切片尚未补齐的部分必须记录在 `docs/current-status.md`，不得被描述为可公开部署。
- 失败请求必须返回可识别的错误码，不得只返回模型厂商的原始错误文本。

## 开发流程

开始任务前：

1. 阅读当前状态和相关 ADR。
2. 检查工作树，保留并避开用户已有修改。
3. 明确本次任务的验收条件和影响范围。

完成任务前：

1. 运行与变更风险相匹配的检查和测试。
2. 修改文档后运行 `npm run check:docs`。
3. 更新受影响的文档。
4. 若产品行为发生变化，更新 `CHANGELOG.md`。
5. 若里程碑、阻塞或下一步发生变化，更新 `docs/current-status.md`。
6. 若模型、提示词或参数经过测试，更新 `docs/model-evaluation-log.md`。
7. 若做出难以轻易逆转的技术决策，在 `docs/decisions/` 新增 ADR。

## 代码规范

- TypeScript 开启严格模式；禁止无理由使用 `any`。
- 组件使用函数式组件；业务逻辑从页面组件中抽离。
- 文件和目录使用 `kebab-case`，React 组件和类型使用 `PascalCase`，变量与函数使用 `camelCase`。
- 异步操作必须处理超时、取消、失败和重试边界。
- 日志必须结构化，不记录 API Key、完整用户图片 Base64、Authorization 请求头或其他敏感数据。
- 新增环境变量时同步更新 `.env.example` 和配置校验，但不得写入真实值。
- 不提交生成图片、用户上传图片、`.env.local`、构建目录或依赖目录。

详细规则见 `docs/development-guide.md`。

## Definition of Done

一次任务只有同时满足以下条件才算完成：

- 功能符合当前产品文档和验收条件。
- 代码通过格式、静态检查和相关测试。
- 错误、空状态、加载状态和重复提交得到处理。
- H5 正常，并且没有无隔离的小程序不兼容实现。
- 没有新增敏感信息泄露或不可控费用风险。
- 文档、状态、变更记录和模型评测记录已按需更新。

## 文档优先级

出现冲突时按以下顺序处理：

1. 用户当前明确要求
2. `AGENTS.md`
3. 已接受的 ADR
4. 产品与架构文档
5. 当前状态和历史记录

不得为了让文档看起来一致而悄悄改变已实现行为；应修正文档或提出新的 ADR。
